#############################################
# Level 4 — Toad's Treasure House (SOLUTION)
#
# Bouwt de JAR en uploadt deze als artifact met een naam die de gebruiker kan opgeven.
#############################################
name: Level 4 - Toad's Treasure House (Solution)
on:
  workflow_dispatch:
    inputs:
      artifact_name:
        description: 'Name of the artifact to upload (e.g. app-jar, demo-artifact)'
        required: false
        default: app-jar

jobs:
  artifact:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build
        run: ./gradlew build

      - name: Upload artifact (JAR)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.artifact_name || 'app-jar' }}
          path: build/libs/*.jar
